server {
    listen 80;
    server_name 54.146.214.123;
    location = /favicon.ico { access_log off; log_not_found off; }
    location /staticfiles/ {
        root /home/ubuntu/EC-Django-Deploy;
    }

    location /media/ {
        root /home/ubuntu/EC-Django-Deploy;
    }

    location / {
        include proxy_params;
        proxy_pass http://unix:/run/gunicorn.sock;
    }
}